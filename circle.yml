machine:
  services:
    - docker
    - couchdb

dependencies:
  override:
  pre:
  - sudo apt-get update; sudo apt-get install chef
deployment:
  production:
    branch: master
    commands:
      - cd chef-repo;ls -a; knife ssh "role:app" "sudo chef-client" -x hamadeh1985 -i ./chef -a ipadr